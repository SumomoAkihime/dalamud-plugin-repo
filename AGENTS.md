# Project Notes

## Known Pitfalls

- In `Ices-Cosmic-Exploration`, mechanical UI translation replacement must distinguish normal string literals from interpolated strings. Replacing the quoted portion of `$"Label"` with `Tr("Label")` produces invalid `$Tr(...)` syntax (`CS1056`). Search for literal `$Tr(` after translation merges and convert non-interpolated labels to `Tr(...)` before building.
- ICE upstream `0.0.78.23` references `WKSMissionModule.MissionRank`, but the Dalamud 15/FFXIVClientStructs environment used in this workspace does not expose that nested enum (`CS0426`). Use the existing `WKSManagerCustom.MissionRank` compatibility enum in `Task_Gather.cs` and `WKSMissionState.cs`.
- ICE upstream `0.0.78.23` also directly accesses protected `WKSManager.State` fields (`CS0122`) for current score and world progress. In this fork, use `WKSManagerCustom.CurrentScore` and `WKSManagerCustom.DevGrade` instead.

- Task requests and real folder names can drift for Dawntrail Savage RM12: requested paths `RM12S1FuturesRewritten` / `RM12S2FuturesRewritten` may not exist while canonical local paths are `BossMod/Modules/Dawntrail/Savage/RM12S1TheLindwurm` and `.../RM12S2TheLindwurm`. Resolve actual filesystem paths first before patching to avoid false "missing directory" assumptions.
- In `ffxiv_bossmod`, `BossMod/Modules/Dawntrail/Savage/M02SHoneyBLovely/` can still exist with debug-only AI files while canonical gameplay module code lives in `.../RM02SHoneyBLovely/`. For low-risk compat work, patch only `RM02` and keep `M02` as namespace/alias bridge only; do not add `ModuleInfo` or duplicate mechanic components in alias layer to avoid canonical+alias double activation or duplicate AID listeners.
- `ffxiv-characterstatus-refined` currently points `origin` to `https://github.com/Kouzukii/ffxiv-characterstatus-refined.git` instead of the maintainer's own GitHub namespace. Confirm or replace the remote before pushing.
- `ffxiv-characterstatus-refined` has an untracked `release/` directory that is not covered by its current `.gitignore`. Treat it as suspicious build output unless there is a clear reason to version it.
- Plugin manifest formats are inconsistent across sibling projects: `WrathCombo` uses `WrathCombo.json`, while `ffxiv-characterstatus-refined` uses `CharacterPanelRefined.yaml`. Any shared publishing/repository tooling must support both.
- `ffxiv-characterstatus-refined` already includes packaged release output (`release/CharacterPanelRefined/latest.zip`), but `WrathCombo` does not currently include equivalent plugin release artifacts or a plugin publish workflow in-repo. Do not assume every plugin here is ready to be listed in a live custom repo without an extra packaging step.
- After rebasing `WrathCombo` to newer upstream commits, `SMN_Helper.cs` can fail with `CS0104` because `AetherFlags` exists in both `Dalamud.Game.ClientState.JobGauge.Enums` and `FFXIVClientStructs.FFXIV.Client.Game.Gauge`. Prefer fully-qualified enum casts in that file to keep CI packaging green.
- `WrathCombo` upstream source repo version metadata may lag behind live published version (e.g., `.csproj` at `1.0.4.0` while `https://love.puni.sh/ment.json` publishes `1.0.4.1`). For CN variant versioning based on upstream release numbers, prefer the source repo index (`ment.json`) as version base instead of the packaged manifest alone.
- Avoid 5-part CN AssemblyVersion strings like `1.0.4.1.n`; Dalamud/.NET version comparison is safer with exactly 4 parts. Use derived 4-part form `a.b.c.(d*1000+n)` (for example upstream `1.0.4.1` + CN `12` -> `1.0.4.1012`).
- In this workspace, invoking `rg.exe` can fail with `Access is denied` even when PowerShell commands work normally. If fast file search unexpectedly fails, fall back to `Get-ChildItem` + `Select-String` instead of assuming missing files.
- `ffxiv_bossmod` hardcoded `Service.LuminaSheet<T>()` to `Lumina.Data.Language.English`; on CN/other non-English environments this can return null sheets and crash plugin init (e.g. `ActionDefinitions.RegisterSpell` NRE during startup). Prefer English-first with fallback to default/client language sheet.
- The root `sync-plugin-repo.yml` workflow fully regenerates `plugin-repo/repo.json` from repositories cloned into `_sources`. If a plugin source repo is not cloned there (or has no built `latest.zip`), that plugin will disappear from the published custom repo on the next sync even if it was manually committed before.
- During cross-repo module merge, some Dawntrail filenames can appear with mojibake in terminal output (for example `Tr?umerei` instead of `Träumerei`) due to console encoding. Use `-LiteralPath` and avoid renaming based on garbled output; verify real file names directly in filesystem before patching.
- `ffxiv_bossmod/BossMod/BossMod.json` can become invalid JSON after encoding-corrupted manual edits (mojibake + missing quote in `Description`). If `Build-DalamudRepo.ps1` fails at `ConvertFrom-Json`, validate this manifest first before troubleshooting build scripts.
- `BossmodReborn` Dawntrail module code currently targets a newer BossMod framework API surface than this fork (for example changed `GenericAOEs.ActiveAOEs` signatures, missing component classes, and enum/category members). Do not bulk-copy Reborn `Modules` into this fork unless framework/API compatibility is aligned first; otherwise compilation fails with hundreds of errors.
- Even when narrowing to seemingly isolated `Dawntrail/Alliance` trash modules (for example `A10Trash`, `A20Trash`), there are still broad helper/API mismatches (`PolygonCustom`, arena helpers, state helper methods, additional component abstractions). Treat these as framework-port tasks, not data-only merges.
- In this fork's geometry API, `RelPolygonWithHoles` / `AddHole` expect concrete `List<WDir>` inputs; passing `CurveApprox.*` enumerables directly can fail to compile. Convert with `.ToList()` when constructing custom AOE polygons.
- `tools/Build-DalamudRepo.ps1` prefers source-side packaged artifacts in `ffxiv_bossmod/release/BossMod/latest.zip` over `plugin-repo/plugins/BossMod/latest.zip`; if that release zip is stale, `repo.json` will keep the old `AssemblyVersion` even after copying a newer plugin zip into `plugin-repo`. Always refresh `ffxiv_bossmod/release/BossMod/latest.zip` from the latest build before regenerating `repo.json`.
- `tools/Build-DalamudRepo.ps1` now requires explicit `-BaseUrl`; running it without that mandatory parameter fails immediately. Reuse the workflow value: `https://raw.githubusercontent.com/SumomoAkihime/dalamud-plugin-repo/master/plugin-repo`.
- Reborn merge conflict resolution can occasionally leave C# files with embedded NUL (`0x00`) bytes (seen in `M07SBruteAbombinatorConfig.cs`), causing garbled display and unreliable diffs. If syntax suddenly looks blank/corrupt, inspect raw bytes and rewrite the file as clean UTF-8 text before building.
- In this workspace, pushing `ffxiv_bossmod` can succeed on GitHub while local tracking ref update fails with `update_ref failed ... refs/remotes/origin/master ... reference broken`. Treat this as local ref corruption (not remote push failure) and verify by checking remote commit presence, then refresh local remote refs as needed.
- `ffxiv_bossmod/EX_MISSING_AFTER_PHASE4.txt` can over-report missing files because several mechanics were already implemented under different filenames/classes in this fork. Before porting from Reborn, normalize the list by checking both file existence and equivalent class/mechanic coverage; otherwise you may attempt duplicate ports and hit unnecessary compile conflicts.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A11Prishe`, Reborn-marked "missing files" (`ArenaChanges.cs`, `AuroralUppercut.cs`, `Explosion.cs`) can already exist as scattered class definitions in other local files (`A11Prishe.cs`, `CrystallineThorns.cs`). Before adding file-parity ports, first scan for existing same-class local implementations to avoid duplicate type errors and accidental behavior forks.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A24Ealdnarche`, Reborn parity checks against `A24Eald'narche` can falsely report missing mechanics when behavior exists under local split names (`TileArena`/`Duplicate1`/`TornadoAttract`) instead of Reborn names (`ArenaChanges`/`Duplicate`/`TornadoPull`). Prefer canonical-folder compat aliases and state activation on alias names; do not create drift directories.
- `ffxiv_bossmod/BossMod/Modules/Shadowbringers/Alliance/A32HanselGretel/A32HanselGretel.cs` existed as an intentionally blank compatibility placeholder. During class/symbol migration checks this can masquerade as a completed port while still missing expected compatibility symbols (`A32HanselGretel`, `WailLamentation`, etc.). Treat blank placeholder modules as unresolved until at least alias/bridge types are provided.
- `ffxiv_bossmod/BossMod/Modules/Shadowbringers/Quest/ASleepDisturbed.cs` uses typo namespace `BossMod.Shadowbringers.Quest.ASleepDistubed` (missing `r`). Symbol-diff tooling that assumes namespace/file-name alignment can misreport missing classes for Reborn `ASleepDisturbed`. Prefer a compatibility namespace bridge instead of renaming the original namespace directly.
- `dotnet build -c Release BossMod.csproj` can fail with `CS2001` for missing source `BossMod/Modules/Shadowbringers/Foray/Skirmish/HeavyBootsOfLead.cs` due to a stale/mismatched compile include list in `BossMod.csproj`. If this appears during unrelated module work, treat it as a project-level blocker and reconcile csproj includes vs filesystem before judging migration changes.
- In this fork, `Dawntrail/Trial` ports from Reborn can still hit hidden API drift even for small self-contained duties (for example `T07Doomtrain`): `GenericAOEs.ActiveAOEs` must return `IEnumerable<AOEInstance>` (not `ReadOnlySpan`), `OnStatusGain` uses `(Actor, ActorStatus)` (not `ref ActorStatus`), and Reborn `Shape/Circle/Donut` arena-geometry helpers may be unavailable. New migration rule: prefer preserving Reborn radar parity as much as possible, including shape, timing, color, arena transitions, and draw order; first add local compatibility helpers or equivalent local geometry/AI code, and use visibility-only downgrades only when exact parity would require risky framework rewrites or explicit user approval. Any unavoidable downgrade must be documented in the migration summary so it can be tested or revisited.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Trial`, a file containing only a C# `using` alias (seen in `T03QueenEternal/T03QueenEternal.cs` and `T05Necron/T05Necron.cs`) does not register a loadable radar module because it declares no `[ModuleInfo]` class. Treat these as compatibility placeholders/partial Reborn migration debris, not migrated radar data, until a real `BossModule` entry exists.
- In `ffxiv_bossmod/BossMod/Modules/Stormblood/Trial/T04Tsukuyomi`, files can exist as tiny compatibility placeholders while Reborn `7.5.1.10` has a real full module. Do not mark this duty migrated by file presence alone; check for `[ModuleInfo]`, state machine, and active component classes before deciding whether to port or skip.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Raid/M05NDancingGreen`, local split files can be placeholder-only while the real logic was compressed into `M05NDancingGreen.cs`. This local compressed port is not behavior-parity with Reborn: it drops/changes Spotlight multi-pattern handling, RideTheWaves second-wave/safe-lane handling, FunkyFloor alternating sets, and several ShapeDistance/AI hint details. Treat M05N as needing a parity repair, not as a completed full Reborn port.
- In this fork, Reborn-derived Dawntrail Alliance ports can also fail on missing helper extensions like `Angle.Round(...)` and `WPos.Quantized()`. For low-risk compatibility migrations, prefer raw `spell.Rotation` / `actor.Rotation` and direct `WPos` math without quantization unless local helper methods are confirmed to exist.
- In `ffxiv_bossmod` Dawntrail Alliance, Reborn-name compatibility files can live in non-obvious sibling duty folders (example: `A32AlexanderCompat.cs` under `A32MedusaSwarmsinger`). Path-only checks can misidentify which canonical duty should receive alias work; verify actual bridged target type before patching to avoid writing compat in the wrong module.

## Dalamud Dev Environment

- Base references for future work:
  - `goatcorp/Dalamud`: framework and API
  - `goatcorp/DalamudPlugins`: legacy official plugin repo format (`plugin.json` + `latest.zip`)
  - `goatcorp/DalamudPluginsD17`: current official manifest repo (`manifest.toml` pointing at a source repository commit)
- The most relevant starter template is `goatcorp/SamplePlugin`, not `DalamudPlugins` itself. Follow its local dev flow unless a project intentionally uses a more advanced custom layout.
- Local machine state checked here: only `.NET SDK 8.0.419` is installed, and `DALAMUD_HOME` is not set.
- Several repos in this workspace target `.NET 10` / `net10.0-windows`, so local builds will fail until a .NET 10-capable SDK is installed.
- SamplePlugin prerequisites from goatcorp:
  - XIVLauncher, FFXIV, and Dalamud must be installed and run at least once.
  - If Dalamud is not in the default location, set `DALAMUD_HOME` explicitly.
  - For default local dev, add the built plugin DLL path in `/xlsettings` -> `Experimental` -> `Dev Plugin Locations`, then enable it from `/xlplugins` -> `Dev Tools`.
- For new or modern plugin projects, prefer the latest `Dalamud.NET.Sdk` in the `.csproj`.
- For advanced/manual packaging flows, `DalamudPackager` is still valid, but then manifest generation and zip contents must be checked carefully.

## Publish Rules

- `goatcorp/DalamudPluginsD17` is the current official submission target. It expects a `manifest.toml` that points to:
  - a public git repository
  - a fixed commit hash
  - the plugin `project_path`
  - owners / maintainers
- `goatcorp/DalamudPlugins` documents the older packaging model and is still useful as a compatibility reference:
  - plugin definition JSON
  - `latest.zip`
  - icon under `images/icon.png`
- Official and custom repos both rely on deterministic versions. Never use timestamp-based or ever-changing build-number versions for `AssemblyVersion`.
- When a custom repo or official repo manifest says one `AssemblyVersion` but the downloadable zip contains another, installs/updates may fail. Prefer reading the packaged plugin manifest from inside `latest.zip` when generating repository metadata.
- New official submissions should go to testing first (`testing/live` in D17, `testing` in the legacy repo flow) before moving to stable.
- Plugin icons should exist in `images/` and stay within the documented size range: minimum `64x64`, maximum `512x512`.
- When a plugin is already distributed from an upstream custom repo that the user still keeps enabled, avoid re-publishing the same `InternalName` from this custom repo unless the goal is to override upstream deliberately. Dalamud may de-duplicate or prefer one source, which makes frequent upstream-sync workflows confusing.
- For upstream-synced forks that still need a distinct listing in the custom repo, prefer a derived variant package with a different `InternalName` instead of overriding the upstream one.
- The root repo now has `.github/workflows/sync-plugin-repo.yml`, which is the preferred refresh path for the hosted custom repo. It pulls active source repositories from GitHub, regenerates `plugin-repo`, and commits the result back to `dalamud-plugin-repo`.
- When searching version strings with PowerShell Get-ChildItem -Recurse | Select-String, unfiltered scans can hit in/ binaries and dump huge garbled output; restrict to source files (for example *.csproj, *.json, *.cs) to avoid misleading noise and terminal slowdowns.
- Root repo now includes .githooks/pre-push to block accidental multi-plugin payload pushes; this guardrail only works after setting core.hooksPath=.githooks (run 	ools/Enable-RepoIsolation.ps1 once per clone).
- Do not run Copy-Item ... latest.zip and 	ools/Build-DalamudRepo.ps1 in parallel: Build-DalamudRepo.ps1 can fail with file-lock IO exception on fxiv_bossmod/release/BossMod/latest.zip. Run these steps sequentially.
- In this fork, BossComponent.KeepOnPhaseChange is a mutable field (set in constructor), not an overridable property; Reborn-style override bool KeepOnPhaseChange will not compile. Also ArenaBoundsCustom has no Center property here, so arena center must be set explicitly (for Ex3 use 
ew(100, 100)).
- If fxiv_bossmod push/fetch reports update_ref failed ... refs/remotes/origin/master ... reference broken, check .git/refs/remotes/origin/master first. In one failure case it contained only NUL bytes (corrupted loose ref) while packed-refs still had an old hash. Minimal fix: delete the corrupted loose ref file, then run git fetch origin --prune to recreate a valid tracking ref.
- If epo.json unexpectedly stays on the previous BossMod version after a successful build, verify that fxiv_bossmod/release/BossMod/latest.zip was copied before running Build-DalamudRepo.ps1; running copy and build steps in parallel can race and leave stale metadata.
- In this terminal environment, git rebase --continue can block waiting for an interactive editor and hit command timeout; use non-interactive continuation ($env:GIT_EDITOR='true'; git rebase --continue) to finish safely.
- Ex4 class-name diff checks can produce false "missing" results if the matcher ignores abstract class declarations (for example Emblazon in Ex4Zelenia/Tiles.cs). When normalizing Reborn/current class names, include abstract classes in the parser before deciding a compatibility alias is needed.
- During upstream merges, if a conflict in `ffxiv_bossmod/BossMod/BossMod.csproj` is left unresolved, later version-bump scripts can silently keep conflict markers (`<<<<<<<`), making builds fail with MSB4025 XML parse errors. Before any version replacement/build, validate the csproj has no conflict markers.
- BossMod versioning policy is upstream-based after each merge: if upstream tag/version is `a.b.c.d`, local continuation must start at `a.b.c.(d*100+1)` and then increment by `+1` per local release. Example: upstream `7.5.0.5` -> first local release `7.5.0.501`, next `7.5.0.502`.
- In this workspace, temporary _tmp_* trees generated from cross-repo snapshots can become undeletable with repeated Access is denied on Remove-Item -Recurse -Force. Prefer avoiding file-materialized snapshots (or isolate temp data in a clearly disposable location) to prevent cleanup stalls.
- In this fork, Components.CastTowers does not expose Reborn-style ActiveTowers(int slot, Actor actor) override points in module classes; attempting to port those overrides fails with CS0115. Keep tower sequencing ports to supported hooks only.
- When comparing Reborn class names via PowerShell + git show, command output can be treated as line arrays and break regex class extraction (false zero/missing results). Normalize with Out-String before regex matching to avoid wrong migration decisions.
- `Ex8Enuo` intermission uses a much larger effective space than the main ring; keeping `ArenaSwitcher` bounds too small clips radar/callout visibility for shadow/add positioning. Prefer wide temporary square bounds during intermission parity work.
- Reborn module files that look self-contained can still depend on newer fork-only API shapes in subtle ways: `uint` where this fork expects typed `Enum` ctor args, `ReadOnlySpan`/new `GenericAOEs` overrides, missing helper components (`InterceptTetherStatus`, `StatusStackSpread`, `Voidzone`), and stricter analyzer failures on imported enum names. For fast completion here, prefer rewriting these as minimal local skeleton modules instead of iterating on direct file ports.
- Directly importing Reborn `Ad011PariofPlenty` split files (`Enums/States/FireFlight/LongNights`) into this fork currently fails hard: local `GenericAOEs` expects `IEnumerable`-based overrides and analyzer/style rules are treated as build errors, so bulk file ports generate dozens of compile/analyzer failures. For this module, prefer incremental visibility-only ports (alias/params/adds display) unless the framework/style baseline is aligned first.

## ffxiv_bossmod 本地构建配置

### 构建环境
- **SDK**: .NET 10.0.203
- **SDK 来源**: `d:\mod-source\_dalamud_tmp\` 目录包含从 Dalamud 发行版提取的预编译 DLL
  - 核心 DLL: `Dalamud.dll`, `Dalamud.Common.dll`, `Dalamud.Bindings.ImGui.dll`, `Dalamud.Bindings.ImPlot.dll`
  - 游戏数据: `FFXIVClientStructs.dll`, `InteropGenerator.Runtime.dll`, `Lumina.dll`, `Lumina.Excel.dll`
  - 辅助库: `ImGuiScene.dll`, `TerraFX.Interop.Windows.dll`
- **环境变量**: 构建前必须设置 `DALAMUD_HOME` 指向 SDK 目录
  ```powershell
  $env:DALAMUD_HOME = "d:\mod-source\_dalamud_tmp"
  ```

### SDK 仓库说明
- `https://github.com/goatcorp/Dalamud` (`d:\mod-source\Dalamud`) — Dalamud 框架**源码**，需要自行编译才能生成 SDK DLL。不直接包含预编译 SDK
- `https://github.com/goatcorp/DalamudPluginsD17` (`d:\mod-source\DalamudPluginsD17`) — 官方插件清单仓库，仅含 `manifest.toml` 元数据，**不含** SDK 文件
- 当前使用的 SDK DLL 来自 Dalamud 发行版构建产物，非从以上仓库直接获取

### 构建命令
```powershell
$env:DALAMUD_HOME = "d:\mod-source\_dalamud_tmp"
dotnet build BossMod\BossMod.csproj -c Release
```

### 构建产物
- `BossMod\bin\Release\BossMod.dll`
- `BossMod\bin\Release\BossMod.json`（插件清单，InternalName: `BossMod`）
- `BossMod\bin\Release\BossMod.pdb`

### 已知 SDK 版本差异
- `_dalamud_tmp` 中的 `FFXIVClientStructs` 版本不包含 `AtkValueType.ConstString` 枚举值，`DebugAddon.cs` 中已注释掉该分支
- 如果后续 SDK 更新，可能需要取消该注释或调整相关代码
- In this fork, `Components.CastLineOfSightAOE` constructor requires explicit `blockersImpassable` argument (`CastLineOfSightAOE(module, aid, range, blockersImpassable, ...)`); Reborn-style shorter constructor calls will fail with CS7036 until adapted.
- In this fork, `Components.SimpleAOEs` does not expose Reborn-side tuning fields like `MaxDangerColor`; attempting to port those assignments causes compile errors. Keep ports to supported constructor parameters only.
- In `ffxiv_bossmod`, analyzer rule IDE0065 is treated as build error; alias `using` directives inside file-scoped namespace blocks will fail Release build. For compatibility alias files, place top-level `using` aliases outside namespace and use fully-qualified type names.
- `ffxiv_bossmod/BossMod/Modules/Dawntrail/Raid/M08NHowlingBlade/` existed as an empty directory (no `.cs` sources), which silently hides a missing-module migration gap while builds still pass. Treat empty module directories as migration blockers and verify file presence, not just folder presence.
- Crash root cause observed in XIVLauncherCN appcrash logs: `BossMod.UIMisc.IconButton` can hard-crash inside `ImRaii.PushFont(Service.IconFont)` / cimgui when opening module-support UI if icon font handle is invalid at draw time. Add null/invalid icon-font fallback path in UI helpers to avoid pushing font blindly.
- In `ffxiv-characterstatus-refined`, after merging upstream commit `0e4b68c` (`Update to FFXIV 7.5 and Dalamud 15`), source version is `1.8.6.0` (`CharacterPanelRefined.csproj`) but existing build outputs can still carry stale `1.8.5.0` metadata (`CharacterPanelRefined/bin/x64/Release/CharacterPanelRefined.json` and `release/CharacterPanelRefined/1.8.5.0`). Do not trust checked-in build artifacts for version alignment; rebuild/package from source before publishing.
- In this workspace, running multiple git mutations against the same repo in parallel (for example `git add` + `git commit` in concurrent tool calls) can leave stale `.git/index.lock` and block further commits. Run repository-mutating git commands sequentially; if blocked, remove the stale lock after confirming no active git process.
- In `ffxiv_bossmod`, Dawntrail dungeon modules still mix older component constructor shapes: many telegraph components are safest via `Components.StandardAOEs`, while `Components.SimpleKnockbacks` may require `uint` action IDs even when nearby components accept enum `AID`. When bulk-porting from Reborn, expect per-component signature drift and downgrade quickly to compile-safe variants.
- This fork is missing several helper components used by Reborn Dawntrail modules (`StretchTetherDuo`, `LineStack`, some `StandardChasingAOEs`/`Exaflare.Line` ctor signatures). For fast ports, replace them with locally available primitives (`SingleTargetCast`, `StackWithCastTargets`, `StandardAOEs`) instead of forcing API-alignment refactors.
- This fork also lacks some higher-level grouped helper wrappers used in Reborn ports (for example `Components.RaidwideCasts`). If a Reborn module only uses those for naming/aggregation, keep the local fork's separate supported components and add a no-op compatibility class only when the class name itself matters for migration bookkeeping.
- Some convenience lifecycle helpers used in Reborn trash modules (`AllDeadOrDestroyed*`, `IsAnyActorInCombat`, and `Arena.Actors(this, uint[])` style overloads) are not available in this fork's module context. For quick compatibility ports, remove those overrides/phase-end predicates first, then add back equivalent logic only if needed.
- If `dotnet build` suddenly reports `MSB4025 Root element is missing` on `BossMod.csproj` or NuGet cache JSON parse errors with `0x00`, check for NUL-byte corruption in `BossMod/BossMod.csproj` and `BossMod/obj/project.assets.json` / `project.nuget.cache`. Minimal recovery: rewrite `BossMod.csproj` from a clean git version, delete `BossMod/obj`, then rebuild.
- XIVLauncherCN crash packs can show repeated Dalamud texture decode failures when plugin manifests/icons are fetched through `gh.atmoomen.top` mirror URLs and the response is not an actual image (`Could not load icon ... The file is not a TexFile`, COM `0x88982F50`). These exceptions can accumulate during `FrameworkUpdate` and coincide with hard crashes. For triage, first disable/remove that mirrored repo path or switch to direct upstream URLs, then retest with minimal plugins.
- During large scripted string replacements for module ports, naive global token replacement (for example replacing `ArenaCenter` / `DefaultArena`) can corrupt already-qualified names and create invalid identifiers (`M06NSugarRiot.M06NSugarRiot...`). Prefer full-file rewrite or AST-aware/manual targeted edits for these migrations.
- `ffxiv_bossmod/UPSTREAM_SYNC_PLAYBOOK.md` in `SumomoAkihime/ffxiv_bossmod` is currently NUL-corrupted in the remote repository itself, so a fresh clone still produces an unreadable file. Do not rely on that file as the sole source of release procedure; cross-check the root `AGENTS.md`, the user-maintained release checklist, and known good command history until the playbook is rewritten as clean UTF-8 text.
- In `ffxiv_bossmod`, some duties can exist both as legacy single-file modules and Reborn-style split directories (example: `BossMod/Modules/Dawntrail/Trial/T01Valigarmanda.cs` vs `BossMod/Modules/Dawntrail/Trial/T01Valigarmanda/*.cs`). Importing split files without checking the existing single-file module causes duplicate type/attribute compile errors (`CS0101`, `CS0579`, `CS8863`). Before porting a duty, search for existing same-namespace duty classes in sibling `.cs` files first.
- Experimental Reborn-baseline branch rule: when using `BossmodReborn` as the source baseline for `ffxiv_bossmod`, restore external plugin identity immediately (`BossMod.csproj`, `AssemblyName=BossMod`, `BossMod.json` `InternalName=BossMod`) and make Windows builds honor `DALAMUD_HOME`; otherwise packaging and local builds silently become `BossModReborn`-shaped. Also keep old-fork DTR behavior (`vbm-*` keys, `AI: On/Off`, safe duplicate-entry fallback) unless the user explicitly approves a Reborn behavior change.
- `BossMod/Modules/Dawntrail/Savage` currently contains both `M05SDancingGreen` and `RM05SDancingGreen` naming for what appears to be the same encounter family (`GroupID=1020`, `NameID=13778` in `RM05SDancingGreen`). `M05SDancingGreen` currently only holds a config class file, while full module implementation lives under `RM05SDancingGreen`; treat this as naming-compatibility debt and verify references before deleting or renaming either path.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance`, Reborn-to-local naming drift can create same-prefix duplicate directories (for example `A31AlZahbi` vs `A31ShantottoTheDemon`, `A32Alexander` vs `A32MedusaSwarmsinger`). When normalizing, keep local BossMod canonical modules (typically with real `NameID` module files) and treat imported same-prefix placeholders as disposable compatibility debris; never trust numeric prefix alone for identity mapping.
- For Shantotto alliance migration, Reborn `A30Shantoto` maps to local canonical `A31ShantottoTheDemon`; the local canonical folder is `A31...` (not `A30...`). Apply mechanism ports/compat updates only under `A31ShantottoTheDemon` and do not create new `A30*` drift directories in this fork.
- Dawntrail `Alliance` folder mapping rule: these are 24-player alliance duties. Reborn numbering commonly starts encounter order at `0` (for example first trash as `...Trash`), while this BossMod fork typically keeps boss encounters and uses order-from-`1` style naming. As a result, direct folder-number matching can be off by one and trash waves may exist only on Reborn side. During migration, verify by actual encounter content/`NameID` and boss identity first; do not map by folder prefix alone.
- Migration tracking exclusion rule (important for backlog reports): if global missing-file diff only contains `Dawntrail/Savage` (`MxxS*` paths) and `Dawntrail/Alliance` (`A22UltimaOmega`, `A23Kam'lanaut`, `A24Eald'narche`, `A30~A36` variants), treat these as naming-drift buckets already handled by local canonical modules/compat placeholders. Do not reopen as new “real missing modules” unless gameplay behavior gaps are explicitly confirmed.
- If build suddenly fails with `Dalamud.NET.Sdk: Dalamud installation not found at ...`, verify that the configured `DALAMUD_HOME` directory actually exists on the current device. A repeatable fix here was: create `D:\mod-source\_dalamud_tmp`, fetch `https://kamori.goats.dev/Dalamud/Release/VersionInfo`, download its `downloadUrl`, extract there, then build with `DALAMUD_HOME=D:\mod-source\_dalamud_tmp`.
- `tools/Build-DalamudRepo.ps1` previously deleted `plugin-repo/plugins` before regeneration, which removed unrelated packaged artifacts whenever a plugin was skipped in the current run. Keep plugin artifact generation incremental (overwrite produced plugins only) to avoid repeated accidental deletions.
- A malformed or mojibake plugin manifest in any sibling project can block `tools/Build-DalamudRepo.ps1` before the intended plugin is published (seen with untracked `Ices-Cosmic-Exploration/ICE/ICE.json` missing closing quotes after Chinese text corruption). Keep repo generation tolerant of per-project metadata failures and skip broken unrelated projects with a warning.
- In PowerShell, `Copy-Item -LiteralPath` does not expand wildcard patterns (`*.cs`), so a copy step can appear successful while copying zero files. For pattern-based imports, use `-Path` (or enumerate files first) and verify destination file count immediately.
- In `BossMod/Modules/Dawntrail/Quantum`, `Q1FinalVerse` and `Q01TheFinalVerse` represent the same encounter family with naming drift. Treat `Q1*` as a compatibility layer targeting `Q01*`; do not remap `Q1FinalVerse` to unrelated modules (for example `DeepDungeon/D99EminentGrief`) just to satisfy missing-file diffs.
- In `ffxiv_bossmod/BossMod/Modules/Heavensward/DeepDungeon`, Reborn uses `DDxx*` files under `DeepDungeon/` root while this fork keeps functional modules under `DeepDungeon/PalaceOfTheDead/` with `Dxx*` names. Treat `DDxx*` as naming-compatibility placeholders only, and avoid duplicate implementation ports by prefix/path alone.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance`, Reborn can use punctuation/wording variants for the same duties (`A22UltimaOmega` vs local `A22OmegaTheOne`, `A23Kam'lanaut` vs `A23Kamlanaut`, `A24Eald'narche` vs `A24Ealdnarche`) plus off-by-one style `A30~A36` folder drift. For migration diffs, normalize by encounter identity and existing local canonical module first; do not create new same-tier directories just to match Reborn spelling.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A23Kamlanaut`, key mechanics can be implemented under `A23KamlanautStates.cs` instead of same-name mechanism files (`ArenaChanges.cs`, `ShieldBash.cs`, `SublimeEstoc.cs`). File-presence-only migration checks can false-report "missing mechanics"; validate class/type presence before porting.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Quest`, Reborn and local can differ by category path and spelling for the same duty files (for example Reborn `MSQ\AFatherFirst.cs`/`MSQ\TheFeatOfBrotherhood.cs` vs local root `AFatherFirst.cs`/`TheFeatOfTheBrotherhood.cs`, and split `E1OtisOathbroken`/`E2Gwyddrud` vs local `Otis`/`Gwyddrud`). Treat these as naming/path drift and keep low-risk placeholder-only parity when filling diff lists.
- `dotnet build` for `ffxiv_bossmod/BossMod` can emit transient `MSB3026` copy-retry warnings when `bin\Release\BossMod.dll` is locked by a running `.NET Host` process (for example plugin host/testing process). If the build continues to `BossMod -> ...` and ends with `0 errors`, treat it as successful-but-contended; avoid misclassifying as migration failure.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail`, cleanup by directory is unsafe for naming-drift buckets: `Alliance`/`Savage` can mix tracked canonical files with untracked placeholder files in the same duty paths. Before deleting migration debris, enumerate explicit untracked paths (`git ls-files --others --exclude-standard ...`) and delete only those files; never `Remove-Item` whole duty directories.
- Dawntrail Alliance naming drift can reappear after repeated migration passes (especially `A22/A23/A24` apostrophe/name variants and `A30+` numbering-shift variants). Maintain an explicit folder mapping record at `ffxiv_bossmod/DAWNTRAIL_ALLIANCE_FOLDER_MAP.md` and treat local canonical folders as source of truth; Reborn-name folders should remain placeholder-only or be excluded from actionable-missing reports.
- Current migration-diff baseline (2026-05-15): raw Reborn-vs-local path diff reports only `Dawntrail/Savage` and `Dawntrail/Alliance`. These are known naming-drift buckets (`MxxS`/`RMxxS` and alliance naming/numbering drift), so actionable migration count should be treated as zero unless a confirmed gameplay behavior gap is identified.
- Reborn `Dawntrail/Savage` currently keeps `M05S~M08S` folder/file naming, while this fork uses `RM05S~RM08S`; direct filename diff will over-report missing files unless name-mapped first. Also note local `RM07SBruteAbominator` folder contains primary module class `RM07SBruteAbombinator` (spelling drift), so compatibility aliases should target class identity, not folder spelling.
- `ffxiv_bossmod` currently also contains a standalone `BossMod/Modules/Dawntrail/Savage/M07SBruteAbombinator/` folder with `M07SBruteAbombinatorConfig.cs` while active implementation lives under `RM07SBruteAbominator`. For RM07 compatibility tasks, treat this as naming-compat debris and avoid adding extra `ModuleInfo`/component wrappers that can cause canonical+alias double activation or duplicate AID listeners.
- For Dawntrail Savage `M01S`~`M04S`, Reborn stores canonical files under `M0xS*` directories while this fork keeps active implementations under `RM0xS*`; direct path-based missing-file diff will report false gaps (including `M0xS*.cs/*Enums.cs/*States.cs`) unless filename-prefix mapping (`M` <-> `RM`) and per-file inlining (for example `ElevateAndEviscerate` logic embedded in `RM01SBlackCat/Mouser.cs`) are normalized first.
- Reborn Dawntrail Savage naming can diverge by prefix/segment (`M09~M12SP*`) while this fork uses `RM09~RM12S*` (`RM12S1/2` split). A literal “same-path same-folder” diff can report false missing files; normalize folder mapping first (`M09->RM09`, `M10->RM10`, `M11->RM11`, `M12SP1->RM12S1`, `M12SP2->RM12S2`) before deciding which compatibility shims to add.
- In `ffxiv_bossmod` RM09 compatibility work, there are currently two parallel naming layers: canonical implementation in `...Savage.RM09SVampFatale` and a partial Reborn-style `...Savage.M09SVampFatale` presence (for example separate config file). For low-risk alias patches, prefer namespace/type bridge shims without `ModuleInfo` so canonical+alias modules do not double-activate or double-register same AID listeners.
- In Dawntrail Alliance canonical mapping, Reborn `A33Awaern` corresponds to local `A34Awaern` (`NameID=14838`), while local `A33*` is `AlexanderResurrected`. For Awaern parity work, always patch canonical `A34Awaern` to avoid editing the wrong duty by folder number alone.
- Local SDK baseline note can become stale: despite previous note saying only `.NET SDK 8.0.419` installed, current builds in this workspace may run with `.NET SDK 10.0.203` (observed in `dotnet build` output on 2026-05-15). When diagnosing build/toolchain issues, trust live `dotnet --info` / build logs over historical environment notes.
- In this workspace, `dotnet build -c Release ...\BossMod.csproj` can intermittently fail at `CodeAnalysis` with `CS2012` because `CodeAnalysis\obj\Release\netstandard2.0\CodeAnalysis.dll` is locked by `VBCSCompiler`. If no source changes explain the failure, retry with `-nodeReuse:false` before treating it as a code regression.
- In `ffxiv_bossmod`, full Release builds can fail for reasons outside Endwalker migration scope due to pre-existing `Shadowbringers` analyzer issues: `CA1708` namespace case-only conflict (`BossMod.Shadowbringers.Alliance.A31KnaveOfHearts` vs `...A31KnaveofHearts`) and `IDE0065` in `A31KnaveofHeartsCompat.cs` / `A32HanselGretel.cs`. Treat these as external blockers unless the task explicitly includes Shadowbringers cleanup.
- In `ffxiv_bossmod/BossMod/Modules/Stormblood/DeepDungeon`, several `DDxx*.cs` files can be comment-only placeholders (seen on `DD10Mojabune.cs` ~ `DD60Suikazura.cs`) that declare no types. This causes symbol-level diffs to report missing `DDxx`/`DDxxStates` even though `Dxx` implementations exist; add explicit alias classes for safe naming-drift bridge.
- In `ffxiv_bossmod` ARR/HW/STB migration areas, many `Alliance`/`Quest` files are path-only placeholders containing comments but no type declarations. These produce large class/symbol missing lists even when folder names match Reborn; treat them as implementation gaps (or intentional stubs), not safe naming-drift alias candidates.
- Build stability note (observed 2026-05-15): full `dotnet build -c Release BossMod.csproj` can intermittently fail on pre-existing `Shadowbringers` files with style analyzer `IDE0004` promoted to errors (for example `Modules/Shadowbringers/Alliance/A32HanselGretel/A32HanselGretel.cs` and `Modules/Shadowbringers/Quest/MSQ/ASleepDisturbed.cs`). Treat these as external blockers when validating ARR/HW/STB symbol-alias batches.
- In this fork, `Components.GenericBaitAway.Bait` does not support Reborn-style named `offset` constructor argument; direct `CurrentBaits.Add(new(..., offset: ...))` ports fail with `CS1739`. For low-risk compatibility migrations, reuse existing local bait components (for example `GuidedMissileBait`) or adapt to supported constructor overloads instead of forcing offset-based GenericBaitAway ports.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Raid/M07NBruteAbombinator`, replay logs show the major arena sequence is initial square -> middle rectangle -> final square. Do not use map effects `0x05`-`0x08` as the first large arena transition; those fire much earlier and appear to represent local field effects. Anchor the first arena change around `NeoBombarianSpecial` / map effect `index=0x00 state=0x00020001`, then return to final square on `index=0x01 state=0x00020001`.
- `BossmodReborn` upstream sync (observed when updating `49e17ab75 -> f911008c7`) included `BossMod/BossModReborn.csproj.lscache` in tracked files. Treat `*.lscache` as tooling/cache noise for migration diffing; do not interpret it as gameplay/module logic change when planning ports.
- `ffxiv_bossmod/BossMod/Modules/Heavensward/Alliance` currently contains many path-complete but code-empty placeholder files (`// Placeholder for Reborn compatibility migration.`). Even though folder/file names look complete, there are no actual `BossModule`/`ModuleInfo` definitions, so no radar can load in-game for those duties. Treat this directory as unresolved implementation backlog, not as migrated data.
- BossMod replay `.log` files can be binary container data, not plaintext. If first 4 bytes are `BLCB`, treat as Brotli-compressed binary replay stream (not chat/combat text log). Decompressed stream starts with FourCC records like `VER ` and `FRAM`; parse with `ReplayParserLog`/`ReplayBuilder` semantics instead of text grep.
- Replay-to-module correlation rule: identify duty by structured markers (`GroupType/GroupID/NameID`, zone/CFC data, primary actor OID/name/action timeline), then map to local module path. Do not rely on filename spelling alone because naming drift exists (for example `M01NBIackCat` path typo-like spelling vs expected `BlackCat`).
- Radar bug intake format (required for fast fix): ask user to provide one replay file plus one short timestamp window and one expected-vs-actual statement.
  - Preferred minimum fields: `ReplayPath`, `ApproxTime` (or `Start~End`), `ModuleGuess` (optional), `Observed` (what radar drew/called), `Expected` (what should happen), `Role/POV` (job + self/target), `Evidence` (screenshot/video optional).
  - If user cannot provide mechanic names, use positional language templates: "at <time>, marker/aoe/tether on <who>, radar showed <X>, should be <Y>".
  - Triaging order: first verify replay can be parsed; second lock duty identity; third extract nearby `CST!/AIE/STA/TETH/ICON` events; fourth compare against module component logic; fifth patch minimal visibility-first behavior.
- `BossMod/Modules/Shadowbringers/Trial` is absent in both `ffxiv_bossmod` and `BossmodReborn` (path not found on both sides). For migration requests phrased as "Foray or Trial", treat this as expected path mismatch and continue within `Foray` instead of opening a missing-folder migration task.
- Release build blocker note (observed 2026-05-19): after migrating `Heavensward/Alliance/A23Headstone`, `dotnet build -c Release BossMod.csproj -nodeReuse:false` can fail on unrelated `A24Ozma` API drift (`WPos.InSquare` missing and `MiniArena.Actor/Actors` overload mismatch requiring color params). Treat this as external baseline issue unless the task explicitly includes fixing `A24Ozma`.
- Release build blocker note (observed 2026-05-19): after migrating `Heavensward/Alliance/A31DeathgazeHollow`, `dotnet build -c Release BossMod.csproj -nodeReuse:false` can fail on unrelated `A33ProtoUltima` missing state type (`CS0246: A33ProtoUltimaStates` not found). Treat this as external baseline break outside A31 task scope unless explicitly asked to repair `A33ProtoUltima`.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A12Fafnir`, Reborn uses OID name `FireVoidzone` while local code names the same OID (`0x1EBCCB`) as `DragonBreath`. During arena-transition ports, match by OID value/event state rather than enum member name to avoid false mismatch and duplicate-entity bugs.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A14ShadowLord`, a mechanic can be "missing by file parity" while already implemented inline in another file (observed: `DarkNebula` logic was inside `CthonicFury.cs` with no standalone `DarkNebula.cs`). Before porting by filename, scan same directory for existing same-class implementations to avoid duplicate-type imports.
- In this workspace, Reborn `Dawntrail/Alliance/A20Trash` has no same-path local counterpart under `ffxiv_bossmod` (local jumps from `A14*` to `A21*`). For low-risk parity requests targeting Reborn `A20Trash`, create/maintain a dedicated local `A20Trash` compatibility directory and avoid remapping its files into `A21FaithboundKirin` by prefix alone.
- In this workspace, Reborn `Dawntrail/Alliance/A10Trash` can also be missing entirely in local `ffxiv_bossmod` (path not found while `A11+` exists). Before running path-based migration commands, verify `A10Trash` folder existence first; otherwise scripts fail with `PathNotFound` and skip all `A10` trash module ports.
- In `ffxiv_bossmod` Dawntrail Alliance migration, `A32MedusaSwarmsinger` (local canonical, `NameID=14834`) and Reborn `A32Alexander` (`NameID=14529`) are different encounter identities despite close numbering. Treat this as numbering drift/identity mismatch: verify `NameID` + mechanic set before porting "missing files", and use compat aliases rather than replacing canonical duty identity.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A31ShantottoTheDemon`, Reborn `A31AlZahbi` currently represents a different unit shape (lightweight wave/trivial module) than local canonical `A31ShantottoTheDemon` (full boss mechanics). For low-risk sync, keep canonical mechanics as single source of truth and add only compatibility aliases in canonical directory; do not co-activate alias+canonical equivalents or add duplicate same-AID listeners.
- In this workspace, Reborn `BossMod/Modules/Dawntrail/Alliance/A34Promathia` maps to local canonical `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A35Promathia` (same encounter identity: `GroupID=1117`, `NameID=14779`). Treat this as numbering drift and do not create a duplicate `A34Promathia` module tree in local fork just for path parity.
- In this workspace, Reborn `BossMod/Modules/Dawntrail/Alliance/A35ShinryuParadox` maps to local canonical `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A36ShinryuParadox` (same encounter identity: `GroupID=1117`, `NameID=14729`). For low-risk parity, keep mechanics in canonical `A36*` only and avoid adding a second active `A35*` module/state machine that could double-listen the same AIDs.
- In this workspace, Reborn can also expose the same final Dawntrail-alliance unit under `A36HollowKing` (with `A36HollowKing*.cs` naming), while local canonical implementation remains `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A36ShinryuParadox`. Treat `A36HollowKing` as naming-compatibility bridge only; do not duplicate mechanics or add parallel state machines/components that would double-activate equivalent mechanics or double-listen identical AIDs.
- For Dawntrail alliance Promathia mapping, Reborn `A34Promathia` may still be skeleton-only (`A34PromathiaStates` = `TrivialPhase` with minimal module metadata/enums) while local canonical `A35Promathia` already has full mechanics. For parity tasks, keep canonical behavior in `A35Promathia` and add only minimal safe naming compat for `A34*` symbols; do not copy/activate duplicate components or add parallel same-AID listeners.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A36ShinryuParadox`, adding a Reborn-style compatibility namespace file can fail Release build on analyzer rules even when logic is correct: `IDE0161` (expects file-scoped namespace) and `CA1707` (underscore-heavy Reborn icon names). Use file-scoped namespace and keep underscore names behind a narrow `#pragma warning disable CA1707` block to preserve canonical symbol parity without broad analyzer suppression.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Savage/RM11STheTyrant`, local code already mixes canonical `RM11*` mechanics and `M11*` compatibility naming in one directory. For Reborn parity work, keep changes alias/compat-only and do not add extra state activations or duplicate listeners on the same `AID`, otherwise canonical+alias paths can double-activate mechanics.
- Release build blocker note (observed 2026-05-19): `dotnet build -c Release BossMod.csproj` can fail before Dawntrail/Savage validation due to pre-existing analyzer error `IDE0065` in `BossMod/Modules/Dawntrail/Savage/RM09SVampFatale/M09SVampFataleNamespaceCompat.cs` (`using` directive inside namespace block). Treat this as an external RM09 baseline issue when validating unrelated RM10 alias/compat patches.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Savage/RM08SHowlingBlade`, file-scoped `using` aliases in `RM08SCompatibilityAliases.cs` are local to that single file and do not create exported types. They can look like naming compatibility coverage while still leaving cross-file symbol compatibility unresolved; prefer explicit shim classes for safe alias/compat work.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Savage/RM01SBlackCat`, existing `M01SBlackCat*.cs` files can appear to complete Reborn naming compatibility while only providing partial stubs/comments under the `...RM01SBlackCat` namespace. For low-risk parity, verify Reborn-facing namespace bridge (`BossMod.Dawntrail.Savage.M01SBlackCat`) exists, and keep it alias-only (no `ModuleInfo`, no new component/state activations) to prevent canonical+alias dual activation or duplicate same-AID listeners.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Savage/RM05SDancingGreen`, comment-only placeholder files (`BackUpDance.cs`, `InsideOutOutsideIn.cs`, etc.) can look like completed Reborn compatibility while exporting no usable symbol bridge. For RM05 low-risk parity, centralize explicit alias-only shims in `RM05SCompatibilityAliases.cs` and keep it free of `ModuleInfo`/state activations to avoid canonical+alias double activation or duplicate same-AID listeners.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Savage/RM06SSugarRiot`, `using`-alias-only shim files (for example `M06SSugarRiot*.cs` or `RM06SCompatibilityAliases.cs`) are file-scoped and do not export compatibility types across files/namespaces. They can appear to provide Reborn naming bridges while actually being inert; for low-risk compat work, use explicit alias bridge classes (no `ModuleInfo`, no extra state activation) to avoid false coverage and canonical+alias duplicate same-AID listeners.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A35Promathia`, an `A34Promathia` compat file implemented as an independent trivial module can look safe but still drifts from canonical behavior over time. Prefer pure `A34 -> A35` symbol bridges (no `ModuleInfo`, no component activation, no extra AID watchers) so naming compatibility cannot fork mechanics or reintroduce canonical+alias double-activation risk.
- In `ffxiv_bossmod/BossMod/Modules/Dawntrail/Alliance/A36ShinryuParadox/A35ShinryuParadoxCompat.cs`, compat `AID` entries currently mix real Shinryu/HollowKing IDs with foreign-looking `5038x` IDs. Treat this file as symbol bridge only; do not wire components/listeners to those compat enums, or alias work can accidentally diverge from canonical behavior and create duplicate same-AID handling risk.
- In `ffxiv_bossmod/BossMod/Modules/Heavensward/Alliance/A25Calofisteri`, local files were pure placeholder stubs (`// Placeholder for Reborn compatibility migration.`) with no `ModuleInfo`/states/components, so the duty was effectively non-runnable despite path presence. For parity backfills, prioritize a minimal runnable baseline (Enums + TrivialPhase + core visibility AOEs/knockback) before any mechanic-depth porting.
- In this fork, `Components.BaitAwayIcon` constructor does not accept Reborn-style named parameter `tankbuster`. Porting code with `tankbuster: true` causes `CS1739`; use supported args (`centerAtTarget`, `activationDelay`, `damageType`) and set tankbuster intent via `damageType: AIHints.PredictedDamageType.Tankbuster`.
- In this fork, `Components.StandardAOEs` constructor overloads are inconsistent across modules; named argument `riskyWithSecondsLeft` can fail with `CS1739` on otherwise valid-looking `StandardAOEs(module, aid, shape/radius, ...)` ports. For low-risk migrations, prefer the basic overload first and only add risk-window tuning after confirming a matching local signature exists.
- In this fork, Reborn-style arena geometry helpers (`Shape` arrays and `ArenaBoundsCustom` constructors with `Rectangle/Square/Circle/Donut` and named args like `AdjustForHitboxInwards`) are not API-compatible. Porting those snippets directly can fail with `CS0246/CS7036/CS1739`. For stable migration, keep Reborn phase-switch timing but downgrade bounds to local `ArenaBoundsRect`/`ArenaBoundsCircle` unless geometry APIs are explicitly aligned first.
- When upgrading a placeholder quest module to a real implementation, check `BossMod/Modules/QuestSymbolCompatibilityBatch*.cs` first. Those files can contain same-namespace stubs for the same duty; if left in place, real ports can fail with duplicate-type errors (`CS0101`, `CS0111`, `CS8863`) until the specific stub block is removed.
- In `Heavensward/Quest/MSQ/DivineIntervention`, Reborn's custom arena pattern (`Capsule`/`Rectangle` arrays + `ArenaBoundsCustom.Center`) is not directly portable in this fork. Use explicit module center with simple `ArenaBoundsCircle/Rect` for low-risk migration, then revisit exact geometry later if needed.
- BossMod plugin startup can fail in `Framework/DTRProvider` if DTR titles (`vbm-autorotation`, `vbm-ai`, `vbm-stats`) are already occupied: `System.ArgumentException: An entry with the same title already exists.` This can happen after failed/ungraceful unloads or parallel BossMod variants. DTR registration must handle duplicate titles gracefully (retry with suffixed keys) instead of aborting plugin construction.
- Migration scope guardrail: if upstream `ffxiv_bossmod` already has a complete implementation for a duty (module data/mechanics usable as-is), do not force-merge or force-complete Reborn data into that duty. Instead, list the potential gap/option items and wait for user decision; execution order and whether to patch are based on user approval.
- `Ices-Cosmic-Exploration` 当前源码在新版 Dalamud/FFXIVClientStructs 下会因 `WKSManager.State` 不可访问和 `AgentWKSMission.GetCriticalMissions` 缺失而构建失败。优先复用项目内 `WKSManagerCustom` 扁平偏移字段，并用已有 `AgentWKSMissionEx.GetCriticalMissions` shim，不要直接改回 `WKSManager.Instance()->State`。
- Dalamud / 插件发布参考链接：基础框架 `https://github.com/goatcorp/Dalamud`；推荐模板 `https://github.com/goatcorp/SamplePlugin`；打包器说明 `https://github.com/goatcorp/DalamudPackager`；插件元数据 `https://dalamud.dev/plugin-development/plugin-metadata/`；自定义仓库发布 `https://dalamud.dev/plugin-publishing/custom-repositories/`；旧式官方仓库格式 `https://github.com/goatcorp/DalamudPlugins`；D17 官方清单仓库 `https://github.com/goatcorp/DalamudPluginsD17`。
- `Ices-Cosmic-Exploration` 中文自定义源不要继续发布为上游同名 `InternalName=ICE`，否则用户同时启用 Puni/上游源时可能因同内部名/同程序集名发生去重或安装状态漂移，表现为重启后又显示未安装。中文变体应使用独立 `AssemblyName/InternalName=ICECN`，并让 workflow 从 `ICE\bin\Release\ICECN\latest.zip` 复制到 `release\ICECN\latest.zip` / `plugin-repo/plugins/ICECN/latest.zip`。
- `Ices-Cosmic-Exploration` 上游源码 `LeontopodiumNivale14/Ices-Cosmic-Exploration` 的 `Main-Branch` 可能落后于 Puni 已发布包版本；2026-06-14 检查时源码仍为 `0.0.78.15` / commit `c96e8c9`，但 `https://puni.sh/api/repository/ice` 已发布 `ICE` `0.0.78.16`。同步上游时先确认源码分支、Puni repo JSON 与实际包版本，不要只看其中一个来源。
- `ffxiv_bossmod` 以 BossmodReborn 为基线合并 awgil autorotation 时，不能只迁移模块文件和 `Execute(..., ref Actor? primaryTarget, ...)` 签名。awgil 的 VBM Default + VBM AI 依赖 `RotationModuleManager` 的多预设叠加模型；若保留 Reborn 单 `Preset?` 模型，AI 会覆盖默认旋转，表现为 VBM Default 与 VBM AI 只能二选一。
- `ffxiv_bossmod` 的 `BossMod/DalamudPackager.targets` 如果在 Release 目标中设置 `MakeZip="false"`，`dotnet build -c Release` 只会生成 DLL/JSON，不会刷新 `BossMod/bin/Release/BossMod/latest.zip`；发布前必须确保 Release 打包目标为 `MakeZip="true"`，否则可能复制旧版本 zip。
- 当前 `D:\mod-source` 同时作为根仓工作区和多插件仓父目录使用，且根仓索引里可能残留 `AD` 临时文件、嵌套插件仓 gitlink、`dalamud-plugin-repo/...` 等历史暂存痕迹。整理目录结构前必须先确认是否要移动根仓 `.git`；在索引脏且路径/磁盘不一致时，不要直接移动 `.git` 或批量搬迁插件仓，以免扩大未提交状态或让根仓误判插件仓为删除/新增。
- 目录结构整理后，发布仓根目录为 `D:\mod-source\dalamud-plugin-repo`，插件源码仓与其平级位于 `D:\mod-source\*`，Dalamud SDK 仍位于 `D:\mod-source\_dalamud_tmp`。`tools/Build-DalamudRepo.ps1` 默认从发布仓父目录扫描插件源码，但输出仍写入发布仓内的 `plugin-repo`；如手动传 `-SourceRoot` / `-OutputRoot`，需保持这一分离。
- 发布仓 `.github/workflows/sync-plugin-repo.yml` 会定时 clone `SumomoAkihime/ffxiv_bossmod` 默认分支并重建 `plugin-repo`。如果手动发布的是 `ffxiv_bossmod/test2` 产物，但源仓 `master` 仍是旧版本，下一次自动同步会把 `repo.json` 和 BossMod zip 覆盖回旧版本。要让发布长期生效，必须同步源仓默认分支，或明确把 workflow 改为 clone 指定测试分支。
- `ffxiv_bossmod/test2` 的 autorotation 多预设迁移中，`PresetDatabase.AllPresets` 不能按 `HideDefaultPresets` 改写或过滤默认预设；否则 `VBM Default` / `VBM AI` 会从 UI 和 AI preset 查找中消失。隐藏 `VBM Default` 应只发生在 Autorotation 窗口按钮层，并且只针对未激活的 `VBM Default`。
- 当前本地 `BossmodReborn` 对照仓没有发现独立中文 UI 翻译资源，源码中的 `ConfigDisplay` / `PropertyDisplay` 多数仍为英文。不要假设“切回 Reborn 基线”会自动恢复中文界面；如需中文 UI，应先确认中文 Reborn 分支/资源来源，再做系统性资源合并，避免手工零散翻译造成维护分叉。
- `ffxiv_bossmod/test2` 以 Reborn 为基线更完整迁移 awgil autorotation 时，不能只整包覆盖 `Autorotation/ActionQueue/ActionTweaks` 后期待直接编译：Reborn/本仓框架缺少或改名了 `AIConfig.Enabled`、`Service.IsDev`、`Utils.SortBy/FirstOrNull/LoadFromAssembly`、`PartyState.TryFindSlot`、`AIHints.SpecialMode.PyreticMove`、部分 duty action 别名等。优先补窄兼容层或同义字段，避免引入 Reborn 底层 framework/network/data 大改。
- `ffxiv_bossmod/test2` 的原版 autorotation 代码会把 `AIHints.GoalSingleTarget/GoalCombined/TargetInAOE*` 当实例方法调用，但当前 `AIHints` 实现中这些是静态方法；迁移时应限定为 `AIHints.*`，不要改 `GoalAOE*` 这类仍为实例方法的调用。
- `ffxiv_bossmod/test2` 的 Reborn AI 控制层和 awgil 多预设 autorotation 叠加时，`/bmrai on/off/toggle` 不仅要切换 `AIBehaviour`，还必须同步 `AIConfig.Enabled` 并触发 `_config.Modified.Fire()`，否则 `RotationModuleManager` 可能不会及时重建/释放 AI preset，表现为 AI 开关状态和实际 autorotation 预设叠加不同步。
- `ffxiv_bossmod/test2` 的 `PlanPresetConverter` 会通过 `Utils.LoadFromAssembly("BossMod.Autorotation.OptionRenames.json")` 读取 `OptionRenames.json`。如果从 awgil 合并该逻辑，必须在 `BossMod.csproj` 中把 `Autorotation\OptionRenames.json` 加为 `EmbeddedResource`；仅把文件放在源码目录或随 zip 输出都不够，旧 preset/schema 转换会在运行时找不到资源。
- `ffxiv_bossmod/test2` 的 autorotation preset/plan JSON 和 IPC transient strategy 接口里保存/传入的是非程序集限定类型名。`Type.GetType(...)` 在运行时可能解析不到同程序集模块/副本类型，导致默认 preset、用户 plan 或外部插件传入的模块名被跳过，游戏里表现为 Autorotation 窗口只剩 `Disabled` 或 IPC 调整失效。读取 preset/plan/manifest/IPC 模块名时必须保留 `typeof(...).Assembly.GetType(...)` fallback。
- `ffxiv_bossmod/test2` 从 Reborn 配置切到 awgil autorotation 配置时，Reborn 字段名是 `HideDefaultPresets`，awgil 字段名是 `HideDefaultPreset`。同版本配置文件不会触发 schema converter，因此 `AutorotationConfig.Deserialize` 需要直接兼容旧字段；否则用户旧配置的隐藏开关状态会静默丢失。
- `ffxiv_bossmod` 做中文 UI 适配时，当前 Dalamud SDK 文档里的 `ClientLanguage` 可能只列出日/英/德/法，不要硬编码不存在的 `ChineseSimplified`/`ChineseTraditional` 枚举。中文检测应优先使用 `ClientState.ClientLanguage`、反射读取的 `DataManager.Language`、Lumina 资料片名称、`CultureInfo` 的字符串信号，并将中文文本放在独立嵌入资源（如 `Localization/zh-CN.json`），避免直接改散落的英文 `ConfigDisplay`/`PropertyDisplay` 文案造成上游冲突。
- `ffxiv_bossmod/test2` 的 `VBM AI` 预设不只是普通辅助技能预设：用户期望它同时具备 Reborn `AI: On` 的自动移动/躲避能力，但又不能和信息栏 AI 开关互相覆盖。实现上应让手动激活的 `VBM AI` 自动拉起一个不拥有预设的 `AIBehaviour` 来驱动移动；不要让该行为反复 `Activate/Deactivate` 用户手动点亮的 `VBM AI` 预设，也不要清掉同时启用的 `VBM Default`。

